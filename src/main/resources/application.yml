spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydbtest?serverTimezone=GMT-6
    username: root
    password: admin12345
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false